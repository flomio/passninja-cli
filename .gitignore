dist/
*.test
*.out
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp

# Local creds — `passninja auth` writes to ~/.passninja-auth.json by default
# but if someone redirects HOME for testing the file may land in-tree.
.passninja-auth.json
.passninja.yaml
